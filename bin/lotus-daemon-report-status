#!/usr/bin/env bash

declare daemon="$HOME/.lotus"
declare host=$(hostname)

if [ ! -f "${daemon}/repo.lock" ]; then
    mail -s "lotus daemon offline" -r bot@ops.sandford.pub team@ops.sandford.pub \
        <<< "lotus daemon is offline. Begin troubleshooting immediately. End report."
fi