#!/usr/bin/env bash

declare markets="$HOME/.lotusmarkets"
declare host=$(hostname)

if [ ! -f "${markets}/repo.lock" ]; then
    mail -s "markets node offline" -r ***REMOVED*** ***REMOVED*** \
        <<< "markets node is offline. Begin troubleshooting immediately. End report."
fi